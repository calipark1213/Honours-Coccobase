---
title: "Syracosphaera models"
author: "Seoyeon Park B00768397"
date: "10/28/2021"
output: html_document
---

```{r}
load("data_subsets.RData")
```

```{r}
library(mgcv)
```


##MODEL G

#Syracosphaera_modG

```{r}
Syracosphaera_modG <- gam(cells_per_litre ~ s(p_an, bs = "tp") + s(si_star, bs = "tp") + s(ni_star, bs = "tp") + s(s_an, bs = "tp") + s(t_an, bs = "tp") + s(scientific_name, bs = "re") + te(longitude, latitude, bs = c("cc","tp")) , data = data.Syracosphaera)
summary(Syracosphaera_modG)
```

##MODEL GS - environmental variables + group smoother scientific_name

#Syracosphaera_modGS_p

```{r}
Syracosphaera_modGS_p <- gam(cells_per_litre ~ s(p_an, bs = "tp") + s(si_star, bs = "tp") + s(ni_star, bs = "tp") + s(s_an, bs = "tp") + s(t_an, bs = "tp") + s(p_an, scientific_name, bs = "fs") + te(longitude, latitude, bs = c("cc", "tp")) + s(scientific_name, bs = "re"), data = data.Syracosphaera, method = "REML")
summary(Syracosphaera_modGS_p)
```

#Syracosphaera_modGS_sistar

```{r}
Syracosphaera_modGS_sistar <- gam(cells_per_litre ~ s(p_an, bs = "tp") + s(si_star, bs = "tp") + s(ni_star, bs = "tp") + s(s_an, bs = "tp") + s(t_an, bs = "tp") + s(si_star, scientific_name, bs = "fs") + te(longitude, latitude, bs = c("cc", "tp")) + s(scientific_name, bs = "re"), data = data.Syracosphaera, method = "REML")
summary(Syracosphaera_modGS_sistar)
```

#Syracosphaera_modGS_nistar

```{r}
Syracosphaera_modGS_nistar <- gam(cells_per_litre ~ s(p_an, bs = "tp") + s(si_star, bs = "tp") + s(ni_star, bs = "tp") + s(s_an, bs = "tp") + s(t_an, bs = "tp") + s(ni_star, scientific_name, bs = "fs") + te(longitude, latitude, bs = c("cc", "tp")) + s(scientific_name, bs = "re"), data = data.Syracosphaera, method = "REML")
summary(Syracosphaera_modGS_nistar)
```

#Syracosphaera_modGS_s

```{r}
Syracosphaera_modGS_s <- gam(cells_per_litre ~ s(p_an, bs = "tp") + s(si_star, bs = "tp") + s(ni_star, bs = "tp") + s(s_an, bs = "tp") + s(t_an, bs = "tp") + s(s_an, scientific_name, bs = "fs") + te(longitude, latitude, bs = c("cc", "tp")) + s(scientific_name, bs = "re"), data = data.Syracosphaera, method = "REML")
summary(Syracosphaera_modGS_s)
```

#Syracosphaera_modGS_t

```{r}
Syracosphaera_modGS_t <- gam(cells_per_litre ~ s(p_an, bs = "tp") + s(si_star, bs = "tp") + s(ni_star, bs = "tp") + s(s_an, bs = "tp") + s(t_an, bs = "tp") + s(t_an, scientific_name, bs = "fs") + te(longitude, latitude, bs = c("cc", "tp")) + s(scientific_name, bs = "re"), data = data.Syracosphaera, method = "REML")
summary(Syracosphaera_modGS_t)
```